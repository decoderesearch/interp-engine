<!-- Generated by tests/models_status.py -- do not edit by hand. -->

# Model support status

Every architecture below is one vLLM can serve, plus the multimodal wrappers the comparison sweep
covers. The list is a snapshot of vLLM 0.26.0's registry
(`tests/vllm_supported_archs.json`): a serving runtime's registry is the only definition of "models
people run" that stays current without anyone curating it. Families vLLM serves through its
transformers backend rather than a native implementation count too -- vLLM serves them either way.

The tiers are **what has been shown**, not a forecast:

- **verified** -- captured on real weights and checked point-by-point against other engines (31 architectures)
- **resolves** -- every point resolves and the arithmetic invariants hold; no independent engine has reproduced it (59 architectures)
- **unaudited** -- not probed: transformers has no class for the family, or its own config defaults do not build (46 architectures)
- **broken** -- a module tree the `(point, layer)` addressing cannot express, with the reason (2 architectures)

The distinction that matters most is between *resolves* and *verified*. Resolution walks the module
tree and matches attribute names, so it catches the failure this engine is most prone to -- a family
that spells `self_attention` where its cousins spell `self_attn` loses that point entirely, on every
checkpoint, silently. What it cannot catch is a point that resolves to the *wrong* module: a fused
`qkv` split by the wrong layout yields a correctly shaped `value` holding a mix of queries and keys,
and only a numeric check against another implementation notices. That is what the sweep is for, and
it is why the top tier is small and per-checkpoint.

## Verified numerically

Captured on real weights and compared point-by-point against the engines named, at the tolerances in
`comparison/spec.py`. `agreeing engines` lists only engines that agreed on **every** checkpoint of the
family. The full matrix, with per-point cosines and the command to reproduce any cell, is the table in
[README.md](../README.md) and the JSON under `comparison/results/`.

| architecture | checkpoints | agreeing engines |
| --- | --- | --- |
| `BloomForCausalLM` | `bigscience/bloom-560m` | tlens_v2, vllm, vllm-static |
| `DeepseekV2ForCausalLM` | `deepseek-ai/DeepSeek-V2-Lite` | nnsight, vllm, vllm-static |
| `GPT2LMHeadModel` | `openai-community/gpt2` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `GPTBigCodeForCausalLM` | `bigcode/gpt_bigcode-santacoder` | nnsight, tlens_v3, vllm, vllm-static |
| `GPTNeoXForCausalLM` | `EleutherAI/pythia-70m-deduped` | nnsight, vllm, vllm-static |
| `Gemma2ForCausalLM` | `google/gemma-2-27b` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `Gemma3ForCausalLM` | `google/gemma-3-1b-it` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `Gemma3ForConditionalGeneration` | `google/gemma-3-27b-it` | nnsight, tlens_v3, vllm, vllm-static |
| `Gemma4ForConditionalGeneration` | `google/gemma-4-31B` | nnsight |
| `GptOssForCausalLM` | `openai/gpt-oss-20b` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `GraniteForCausalLM` | `ibm-granite/granite-3.3-2b-instruct` | nnsight, vllm, vllm-static |
| `GraniteMoeForCausalLM` | `ibm-granite/granite-3.0-1b-a400m-base` | nnsight, vllm, vllm-static |
| `Lfm2ForCausalLM` | `LiquidAI/LFM2.5-230M` | nnsight, tlens_v3, vllm, vllm-static |
| `Lfm2MoeForCausalLM` | `LiquidAI/LFM2-8B-A1B` | nnsight, tlens_v3, vllm, vllm-static |
| `LlamaForCausalLM` | `meta-llama/Llama-3.1-8B`<br>`meta-llama/Llama-3.3-70B-Instruct` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `MistralForCausalLM` | `mistralai/Mistral-7B-v0.1` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `NemotronHForCausalLM` | `nvidia/NVIDIA-Nemotron-3-Nano-4B-BF16` | nnsight, tlens_v3, vllm, vllm-static |
| `OPTForCausalLM` | `facebook/opt-125m` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `Olmo2ForCausalLM` | `allenai/OLMo-2-0425-1B` | nnsight, vllm, vllm-static |
| `Olmo3ForCausalLM` | `allenai/Olmo-3-1125-32B` | nnsight, vllm, vllm-static |
| `Phi3ForCausalLM` | `microsoft/Phi-3-mini-4k-instruct` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `PhiForCausalLM` | `microsoft/phi-2` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `PhiMoEForCausalLM` | `microsoft/Phi-mini-MoE-instruct` | nnsight, tlens_v3, vllm, vllm-static |
| `Qwen2ForCausalLM` | `Qwen/Qwen2.5-7B-Instruct` | nnsight, tlens_v2, tlens_v3, vllm, vllm-static |
| `Qwen3ForCausalLM` | `Qwen/Qwen3-32B` | nnsight, tlens_v3, vllm, vllm-static |
| `Qwen3MoeForCausalLM` | `Qwen/Qwen3-30B-A3B` | nnsight, tlens_v3, vllm, vllm-static |
| `Qwen3NextForCausalLM` | `Qwen/Qwen3-Next-80B-A3B-Instruct` | nnsight, tlens_v3, vllm, vllm-static |
| `Qwen3_5ForConditionalGeneration` | `Qwen/Qwen3.8-27B` | nnsight, tlens_v3, vllm, vllm-static |
| `Qwen3_5MoeForConditionalGeneration` | `Qwen/Qwen3.6-35B-A3B` | nnsight, tlens_v3, vllm |
| `SmolLM3ForCausalLM` | `HuggingFaceTB/SmolLM3-3B` | nnsight, tlens_v3, vllm, vllm-static |
| `Starcoder2ForCausalLM` | `bigcode/starcoder2-3b` | nnsight, tlens_v3, vllm, vllm-static |

A verified architecture is evidence about its **family**, not only about the checkpoints listed: the
module tree and the names are shared, so a same-family checkpoint at another size differs in dims and
in nothing that resolution depends on. Different *generations* are a different matter (Gemma-2 and
Gemma-3 disagree about where their norms are), which is why each generation appears on its own row.

## Swept, and nothing independent came back

Not a tier -- each family below keeps whichever tier the audit gives it -- but the distinction the
tiers cannot draw: these were *tried*. Each has committed results, so a cell exists, but no
independently written engine finished. The comparisons errored, crashed or were skipped as
unsupported, leaving interp-engine's own two backends as the only ones that captured, and those
two share this repository's reading of the architecture. Their agreement is not evidence, so these
are not verified; the failing runs are the thing to fix, and they are easy to lose sight of once
the family is filed under a weaker claim.

| architecture | checkpoints |
| --- | --- |
| `DeepseekV4ForCausalLM` | `deepseek-ai/DeepSeek-V4-Flash-0731` |

## Resolves, unverified numerically

Every canonical point resolves against the real module tree, or is refused with an explanation, and
the three invariants below hold on a tiny randomly initialized model of the family. Resolution is
checked on the `meta` device from each family's config defaults; the invariants run a real forward on
a shrunk config. No weights are downloaded for either. `absent points` are the ones refused -- read
the legend below before reading them as gaps.

| architecture | absent points | invariants |
| --- | --- | --- |
| `AfmoeForCausalLM` | -- | hold |
| `ApertusForCausalLM` | `mlp_pre_linear` | hold |
| `ArceeForCausalLM` | `mlp_pre_linear` | hold |
| `Cohere2ForCausalLM` | `resid_mid` | hold |
| `Cohere2MoeForCausalLM` | `resid_mid`, `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `CohereForCausalLM` | `resid_mid` | hold |
| `CwmForCausalLM` | -- | hold |
| `DeepseekV32ForCausalLM` | `value` | hold |
| `DeepseekV3ForCausalLM` | `value` | hold |
| `Ernie4_5ForCausalLM` | -- | hold |
| `Ernie4_5_MoeForCausalLM` | -- | hold |
| `Exaone4ForCausalLM` | -- | hold |
| `ExaoneMoEForCausalLM` | -- | hold |
| `Fairseq2LlamaForCausalLM` | -- | hold |
| `FalconForCausalLM` | `resid_mid`, `mlp_pre_linear` | hold |
| `FalconH1ForCausalLM` | -- | all but `residual` |
| `FalconMambaForCausalLM` | `resid_mid`, `value`, `mlp_pre`, `mlp_pre_linear`, `mlp_act`, `attn_in`, `attn_out`, `attn_out_post`, `z`, `mlp_in`, `mlp_out`, `mlp_out_post` | hold |
| `FlexOlmoForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `GPTJForCausalLM` | `resid_mid`, `mlp_pre_linear` | hold |
| `Gemma3nForCausalLM` | -- | not checked: its `layer_types` list is per-layer and validated against the full 35-layer pattern |
| `Gemma4ForCausalLM` | -- | not checked: same per-layer validation as Gemma-3n |
| `GemmaForCausalLM` | -- | hold |
| `Glm4ForCausalLM` | `mlp_pre`, `mlp_pre_linear` | hold |
| `Glm4MoeForCausalLM` | -- | hold |
| `Glm4MoeLiteForCausalLM` | `value` | hold |
| `GlmForCausalLM` | `mlp_pre`, `mlp_pre_linear` | hold |
| `GlmMoeDsaForCausalLM` | `value` | hold |
| `GraniteMoeHybridForCausalLM` | `value`, `mlp_pre`, `mlp_pre_linear`, `mlp_act`, `attn_in`, `attn_out`, `attn_out_post`, `z` | hold |
| `GraniteMoeSharedForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `GritLM` | -- | hold |
| `HYV3ForCausalLM` | -- | hold |
| `HyperCLOVAXForCausalLM` | -- | hold |
| `InklingForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `InklingForConditionalGeneration` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | not checked: the multimodal wrapper sizes its vision tower from fields this table does not shrink |
| `Jais2ForCausalLM` | `mlp_pre_linear` | hold |
| `JambaForCausalLM` | -- | hold |
| `LLaMAForCausalLM` | -- | hold |
| `LagunaForCausalLM` | -- | hold |
| `Llama4ForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `LongcatFlashForCausalLM` | `value` | not checked: its block count is derived |
| `MPTForCausalLM` | `mlp_pre_linear` | hold |
| `Mamba2ForCausalLM` | `resid_mid`, `value`, `mlp_pre`, `mlp_pre_linear`, `mlp_act`, `attn_in`, `attn_out`, `attn_out_post`, `z`, `mlp_in`, `mlp_out`, `mlp_out_post` | not checked: `num_heads` for the SSM is independent of `num_attention_heads` and the default does not divide this hidden size |
| `MambaForCausalLM` | `resid_mid`, `value`, `mlp_pre`, `mlp_pre_linear`, `mlp_act`, `attn_in`, `attn_out`, `attn_out_post`, `z`, `mlp_in`, `mlp_out`, `mlp_out_post` | hold |
| `MellumForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `MiMoV2FlashForCausalLM` | -- | hold |
| `MiniCPM3ForCausalLM` | `value` | hold |
| `MiniMaxM2ForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `Ministral3ForCausalLM` | -- | hold |
| `MixtralForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `MptForCausalLM` | `mlp_pre_linear` | hold |
| `OlmoForCausalLM` | -- | hold |
| `OlmoHybridForCausalLM` | -- | hold |
| `OlmoeForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `Qwen2MoeForCausalLM` | `mlp_pre`, `mlp_pre_linear`, `mlp_act` | hold |
| `RWForCausalLM` | `resid_mid`, `mlp_pre_linear` | hold |
| `SeedOssForCausalLM` | -- | hold |
| `StableLMEpochForCausalLM` | -- | hold |
| `StableLmForCausalLM` | -- | hold |
| `Zamba2ForCausalLM` | `resid_mid`, `mlp_pre`, `mlp_pre_linear` | not checked: `layer_types` is a validated dataclass field whose default pattern is longer than a 2-layer trunk |

Which points are absent says which shape the MLP has, and the three cases are worth telling apart:

- **`mlp_pre_linear` alone** -- a plain (ungated) MLP: `up_proj` then activation then `down_proj`, with
  no second branch to multiply, so the point names nothing. GPT-2, OPT, BLOOM, MPT, GPT-J, phi-2.
- **`mlp_pre` and `mlp_pre_linear`** -- a gated MLP whose gate and up projections are *one fused
  matrix* (`gate_up_proj`), so neither branch is a module output. `mlp_act` survives, since it is the
  down projection's input either way -- and that is the neuron basis SAEs are trained on.
- **all three** -- a sparse mixture of experts, where the projections live on the experts rather than
  at the block boundary. Those families report `router_logits` / `expert_weights` / `expert_indices`
  instead, which is the information `mlp_out` cannot recover.

And the other two:

- **`value`** -- multi-head latent attention (DeepSeek-V2/V3, MiniCPM3, GLM-MoE) keeps a compressed kv
  latent and expands it inside the forward, so no module's output is the value. `z` still exists; it is
  direct feature attribution that is unavailable.
- **`resid_mid`** -- a parallel block (GPT-NeoX, Falcon, GPT-J, phi-2, Cohere) has no residual *between*
  its sublayers, because nothing sequences them. A single-sublayer block (Nemotron-H) has none either.

A family missing the attention or MLP points entirely is a state-space or all-linear-attention trunk,
which the next table records explicitly.

### Points an architecture does not have

Recorded facts rather than gaps, from `family_coverage.ARCHITECTURAL_ABSENCES`. Everything else about
these families works, including the residual stream, which is where interpretability on them happens.

| architecture | absent | why |
| --- | --- | --- |
| `FalconMambaForCausalLM` | `attn_in`, `attn_out`, `attn_out_post`, `mlp_in`, `mlp_out`, `mlp_out_post`, `z` | a FalconMambaBlock is `norm` + `mixer` |
| `GraniteMoeHybridForCausalLM` | `attn_in`, `attn_out`, `attn_out_post`, `z` | the *default config* is all `linear_attention`, so this probe's trunk has no attending layer -- a shipped granite-4.0-h checkpoint interleaves `full_attention` and those layers resolve, which `test_granite_hybrid_attention_resolves_where_a_checkpoint_has_it` shows |
| `Mamba2ForCausalLM` | `attn_in`, `attn_out`, `attn_out_post`, `mlp_in`, `mlp_out`, `mlp_out_post`, `z` | a Mamba2Block is `norm` + `mixer`: neither sublayer exists |
| `MambaForCausalLM` | `attn_in`, `attn_out`, `attn_out_post`, `mlp_in`, `mlp_out`, `mlp_out_post`, `z` | a MambaBlock is `norm` + `mixer`: neither sublayer exists |

### Attention with no separable value (no DFA)

| architecture | why |
| --- | --- |
| `AXK1ForCausalLM` | MLA: kv latent expanded in the forward |
| `DeepseekV2ForCausalLM` | MLA: kv latent expanded in the forward |
| `DeepseekV32ForCausalLM` | MLA: kv latent expanded in the forward |
| `DeepseekV3ForCausalLM` | MLA: kv latent expanded in the forward |
| `DeepseekV4ForCausalLM` | MLA: kv latent expanded in the forward |
| `Glm4MoeLiteForCausalLM` | MLA: kv latent expanded in the forward |
| `GlmMoeDsaForCausalLM` | MLA: kv latent expanded in the forward |
| `LongcatFlashForCausalLM` | MLA: kv latent expanded in the forward |
| `MiniCPM3ForCausalLM` | MLA: kv latent expanded in the forward |

### The three invariants

Each fails loudly on the misbinding resolution cannot catch, and none needs a second framework, so all
three run over every family in this tier (`tests/test_invariants.py`, on tiny random models):

1. **DFA** -- `attn_probs @ value == z` per head. Catches a wrong fused-qkv layout, a mis-split head, a
   `value` that is really a key, and a value the family rescales after projecting it.
2. **residual decomposition** -- `resid_pre + attn_out_post + mlp_out_post == resid_post`. Catches a
   post-sublayer norm read from the wrong side of an add, a sublayer whose output already includes the
   residual, and a residual multiplier left unapplied.
3. **neuron basis** -- `down_proj(mlp_act) == mlp_out`. Catches a neuron basis resolved to the wrong
   projection of a gated MLP.

Random weights are enough because these are identities the *architecture* satisfies at any weights, and
a family whose quantity does not exist is expected to refuse with a reason -- a refusal passes, a wrong
number does not. What random weights cannot reach is anything a trained config turns on: Granite's
`residual_multiplier` is 1.0 by default and 0.22 on the released checkpoints, so that case is set
explicitly by a test rather than left to the sweep.

### What would promote a family to verified

Agreement with an independent implementation on real weights: a run of the comparison sweep
(`comparison/`) against TransformerLens, nnsight, vLLM or SGLang on a checkpoint of the family. The
invariants above are self-consistency -- they prove the engine's points describe one coherent
computation, not that the computation is the one another framework performs.

## Unaudited

Nothing was probed on these, so there is no claim either way -- and for most of them the likely answer
is that they load. Two reasons, and they are different:

| architecture | why not probed |
| --- | --- |
| `AXK1ForCausalLM` | `no_transformers_class` |
| `ArcticForCausalLM` | `no_transformers_class` |
| `BailingMoeForCausalLM` | `no_transformers_class` |
| `BailingMoeV2ForCausalLM` | `no_transformers_class` |
| `BailingMoeV2_5ForCausalLM` | `no_transformers_class` |
| `ChatGLMForConditionalGeneration` | `no_transformers_class` |
| `ChatGLMModel` | `no_transformers_class` |
| `DbrxForCausalLM` | `not_buildable`: `AttributeError: 'DbrxAttentionConfig' object has no attribute 'rope_theta'` |
| `DeciLMForCausalLM` | `no_transformers_class` |
| `DeepseekForCausalLM` | `no_transformers_class` |
| `ExaoneForCausalLM` | `no_transformers_class` |
| `HCXVisionForCausalLM` | `no_transformers_class` |
| `HCXVisionV2ForCausalLM` | `no_transformers_class` |
| `HunYuanDenseV1ForCausalLM` | `not_buildable`: `TypeError: unsupported operand type(s) for ** or pow(): 'NoneType' and 'float'` |
| `HunYuanMoEV1ForCausalLM` | `not_buildable`: `TypeError: unsupported operand type(s) for ** or pow(): 'NoneType' and 'float'` |
| `IQuestCoderForCausalLM` | `no_transformers_class` |
| `IQuestLoopCoderForCausalLM` | `no_transformers_class` |
| `InternLM2ForCausalLM` | `no_transformers_class` |
| `InternLM3ForCausalLM` | `no_transformers_class` |
| `KimiLinearForCausalLM` | `no_transformers_class` |
| `LongcatFlashNgramForCausalLM` | `no_transformers_class` |
| `MiMoForCausalLM` | `no_transformers_class` |
| `MiMoV2ForCausalLM` | `no_transformers_class` |
| `MiniCPMForCausalLM` | `no_transformers_class` |
| `MiniMaxM3SparseForCausalLM` | `no_transformers_class` |
| `MistralLarge3ForCausalLM` | `no_transformers_class` |
| `NemotronForCausalLM` | `not_buildable`: `TypeError: unsupported operand type(s) for //: 'int' and 'NoneType'` |
| `NemotronHPuzzleForCausalLM` | `no_transformers_class` |
| `OrionForCausalLM` | `no_transformers_class` |
| `OuroForCausalLM` | `no_transformers_class` |
| `PanguEmbeddedForCausalLM` | `no_transformers_class` |
| `PanguProMoEV2ForCausalLM` | `no_transformers_class` |
| `PanguUltraMoEForCausalLM` | `no_transformers_class` |
| `Param2MoEForCausalLM` | `no_transformers_class` |
| `Plamo2ForCausalLM` | `no_transformers_class` |
| `Plamo3ForCausalLM` | `no_transformers_class` |
| `Rnj1ForCausalLM` | `no_transformers_class` |
| `SarvamMLAForCausalLM` | `no_transformers_class` |
| `SarvamMoEForCausalLM` | `no_transformers_class` |
| `SolarForCausalLM` | `no_transformers_class` |
| `Step1ForCausalLM` | `no_transformers_class` |
| `Step3TextForCausalLM` | `no_transformers_class` |
| `Step3p5ForCausalLM` | `no_transformers_class` |
| `TeleChat2ForCausalLM` | `no_transformers_class` |
| `TeleChat3ForCausalLM` | `no_transformers_class` |
| `TeleFLMForCausalLM` | `no_transformers_class` |

- **`no_transformers_class`** -- vLLM implements the family itself and its HF checkpoints ship their
  modeling code (`trust_remote_code`), so there is no class to build from a config. The eager backend
  loads such checkpoints and the vLLM backend resolves against vLLM's own tree; whether either finds
  its modules depends on the names, which is answerable only by downloading code or weights. Where a
  family's names have been read off its checkpoint and added, that claim is checked against a synthetic
  tree in `tests/test_vllm_only_families.py` -- InternLM2 is the worked example.
- **`not_buildable`** -- the family's own config defaults do not survive its `__init__` (a rope field
  left `None`, an attention sub-config missing `rope_theta`). A transformers-side limit, not ours: a
  real checkpoint's config carries those fields, so these are likely to work.

## Does not work

Each of these is a *shape*, not a missing name -- adding a spelling would not help, because the
canonical points do not describe what the tree does. Fixing one means deciding what a point should
mean there.

| architecture | why |
| --- | --- |
| `DeepseekV4ForCausalLM` | the trunk carries `hc_mult` residual streams rather than one (manifold-constrained hyper-connections), so no tensor between the blocks is the residual stream the three resid points name -- see `test_a_hyper_connection_trunk_refuses_the_residual_points`. The engine can now address one (`Address('resid_post', 5, stream=2)`) and has family points for the collapse/write/mix tensors, but this probe asks for the bare point, which is still refused and should be: there is no default stream. vLLM serves all seven of those family points, measured on V4-Flash at layers 0/21/42, and still cannot address a stream through the resid points themselves -- which is a fact about the residual rather than about the wire. Capture `resid_streams` and index it instead. Note that vLLM's decoder layer also returns a (num_tokens, hc_mult, hidden_size) stack and it is NOT that point: vLLM defers each sublayer's write into the next sublayer's kernel, so the returned stack is the one the MLP read rather than the one the block produced -- resid_mid in stream form. The block's output stack reaches no boundary at all and is read off the next layer's kernel; see interp_engine.vllm_capture.mhc and interp_engine.residual_basis.vllm_residual_basis |
| `HrmTextForCausalLM` | the model does not load: trunk discovery wants one `.layers`/`.h`/`.blocks` and finds two stacks side by side (`model.H_module.layers` / `model.L_module.layers`), so it raises `Could not locate transformer trunk` and every point is lost at once, before addressing is reached. The narrower reason this entry used to give -- that a layer index cannot name a position in a single pass -- no longer holds: flattened order counts re-entries, and `(H_cycles * (L_cycles + 1)) * num_layers_per_stack == num_hidden_layers == 128` on the default config, so the indices do line up. What is left is the loader and the re-entry support behind it; until then the flattening tripwire refuses rather than mis-indexing |

## Keeping this current

- `tests/test_models_status.py` fails when the committed doc no longer matches its sources: a new
  architecture in the vLLM snapshot, a family that changed tier, a sweep checkpoint added or removed.
- `tests/test_family_coverage.py` fails when a family stops resolving, and *also* when one of the three
  tables above starts passing -- a fixed entry has to be deleted, so a stale "known gap" cannot
  survive its own fix.
- Regenerate with `PYTHONPATH=. .venv-cmp/bin/python tests/models_status.py`. Refresh the registry
  snapshot from a vLLM venv after an upgrade: `.venv-vllm/bin/python tests/family_coverage.py
  --refresh`.
