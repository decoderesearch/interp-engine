{
  "captured_at": "2026-08-12",
  "device": "cuda",
  "dtype": "bfloat16",
  "engine": "tlens_v3",
  "hf_id": "LiquidAI/LFM2-8B-A1B",
  "known_bug": null,
  "points": {
    "attn_out.18": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 18,
      "missing": "engine",
      "point": "attn_out",
      "status": "N/A"
    },
    "attn_out.2": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 2,
      "missing": "engine",
      "point": "attn_out",
      "status": "N/A"
    },
    "attn_out_post.18": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 18,
      "missing": "engine",
      "point": "attn_out_post",
      "status": "N/A"
    },
    "attn_out_post.2": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 2,
      "missing": "engine",
      "point": "attn_out_post",
      "status": "N/A"
    },
    "mlp_act.0": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 0,
      "missing": "engine",
      "point": "mlp_act",
      "status": "N/A"
    },
    "mlp_out.0": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 0,
      "missing": "engine",
      "point": "mlp_out",
      "status": "N/A"
    },
    "mlp_out.12": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 12,
      "missing": "engine",
      "point": "mlp_out",
      "status": "N/A"
    },
    "mlp_out.18": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 18,
      "missing": "engine",
      "point": "mlp_out",
      "status": "N/A"
    },
    "mlp_out.2": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 2,
      "missing": "engine",
      "point": "mlp_out",
      "status": "N/A"
    },
    "mlp_out.23": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 23,
      "missing": "engine",
      "point": "mlp_out",
      "status": "N/A"
    },
    "mlp_out_post.0": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 0,
      "missing": "engine",
      "point": "mlp_out_post",
      "status": "N/A"
    },
    "mlp_out_post.12": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 12,
      "missing": "engine",
      "point": "mlp_out_post",
      "status": "N/A"
    },
    "mlp_out_post.18": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 18,
      "missing": "engine",
      "point": "mlp_out_post",
      "status": "N/A"
    },
    "mlp_out_post.2": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 2,
      "missing": "engine",
      "point": "mlp_out_post",
      "status": "N/A"
    },
    "mlp_out_post.23": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 23,
      "missing": "engine",
      "point": "mlp_out_post",
      "status": "N/A"
    },
    "mlp_pre.0": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 0,
      "missing": "engine",
      "point": "mlp_pre",
      "status": "N/A"
    },
    "mlp_pre_linear.0": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 0,
      "missing": "engine",
      "point": "mlp_pre_linear",
      "status": "N/A"
    },
    "resid_mid.0": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 0,
      "missing": "engine",
      "point": "resid_mid",
      "status": "N/A"
    },
    "resid_mid.12": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 12,
      "missing": "engine",
      "point": "resid_mid",
      "status": "N/A"
    },
    "resid_mid.18": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 18,
      "missing": "engine",
      "point": "resid_mid",
      "status": "N/A"
    },
    "resid_mid.2": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 2,
      "missing": "engine",
      "point": "resid_mid",
      "status": "N/A"
    },
    "resid_mid.23": {
      "expected": "the bridge has no component map for these architectures, so a block bridges to `hook_in`/`hook_out` and nothing else -- checked on the model: `blocks.2` of the LFM2 MoE has no `attn`, `mlp` or `ln` child, and Nemotron-H's has only `norm`/`mixer`. `resid_post` is the one point a block-level hook can serve, and it is the one that scores. Architecture-shaped rather than checkpoint-shaped: `LFM2.5-230M`, the dense model of the same family, is mapped and delivers every point",
      "layer": 23,
      "missing": "engine",
      "point": "resid_mid",
      "status": "N/A"
    },
    "resid_post.0": {
      "cos": 0.9999999999999999,
      "cos_worst_token": 0.9999999999999998,
      "layer": 0,
      "max_abs_diff": 0.0,
      "point": "resid_post",
      "rel_diff": 0.0,
      "rel_worst_token": 0.0,
      "status": "PASS",
      "tier": "tlens",
      "worst_token": 6
    },
    "resid_post.12": {
      "cos": 1.0,
      "cos_worst_token": 0.9999999999999998,
      "layer": 12,
      "max_abs_diff": 0.0,
      "point": "resid_post",
      "rel_diff": 0.0,
      "rel_worst_token": 0.0,
      "status": "PASS",
      "tier": "tlens",
      "worst_token": 0
    },
    "resid_post.18": {
      "cos": 1.0,
      "cos_worst_token": 0.9999999999999998,
      "layer": 18,
      "max_abs_diff": 0.0,
      "point": "resid_post",
      "rel_diff": 0.0,
      "rel_worst_token": 0.0,
      "status": "PASS",
      "tier": "tlens",
      "worst_token": 2
    },
    "resid_post.2": {
      "cos": 1.0000000000000002,
      "cos_worst_token": 0.9999999999999999,
      "layer": 2,
      "max_abs_diff": 0.0,
      "point": "resid_post",
      "rel_diff": 0.0,
      "rel_worst_token": 0.0,
      "status": "PASS",
      "tier": "tlens",
      "worst_token": 6
    },
    "resid_post.23": {
      "cos": 1.0000000000000002,
      "cos_worst_token": 0.9999999999999998,
      "layer": 23,
      "max_abs_diff": 0.0,
      "point": "resid_post",
      "rel_diff": 0.0,
      "rel_worst_token": 0.0,
      "status": "PASS",
      "tier": "tlens",
      "worst_token": 10
    }
  },
  "reason": "",
  "reference": "eager",
  "run": {
    "date_utc": "2026-08-12T03:31:40+00:00",
    "dtype_note": "engines load the checkpoint's native dtype (dtype='auto'); SGLang serves bf16 only (float32 unsupported), so a float32-native model's SGLang cell is a cross-dtype fused case.",
    "eager_only": false,
    "gpu": {
      "memory_total": "183359 MiB",
      "name": "NVIDIA B200"
    },
    "native_dtype": "bfloat16",
    "reference_status": {
      "device": "cuda",
      "dtype": "bfloat16",
      "reason": "",
      "status": "ok"
    },
    "replicate": [
      "export HF_TOKEN=$(grep -m1 '^HF_TOKEN=' .env | cut -d= -f2- | tr -d '\"')",
      "PYTHONPATH=. .venv-cmp/bin/python -m comparison.tokenize_inputs --dumps dumps --models LiquidAI/LFM2-8B-A1B",
      "PYTHONPATH=. .venv-cmp/bin/python -m comparison.run_engine --engine eager --dumps dumps --model LiquidAI/LFM2-8B-A1B --device cuda",
      "PYTHONPATH=. .venv-cmp/bin/python -m comparison.run_engine --engine tlens_v3 --dumps dumps --model LiquidAI/LFM2-8B-A1B --device cuda",
      "PYTHONPATH=. .venv-cmp/bin/python -m comparison.aggregate --dumps dumps"
    ]
  },
  "sae": {},
  "status": "ok",
  "verdict": "\u2705",
  "versions": {
    "torch": {
      "version": "2.13.0"
    },
    "transformer_lens": {
      "version": "3.7.0"
    },
    "transformers": {
      "version": "5.15.0"
    }
  }
}